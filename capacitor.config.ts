import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rungo.app',   // identificador único de tu app
  appName: 'run_rungo',     // nombre visible de la app
  webDir: 'out'             // carpeta exportada por Next.js
};

export default config;